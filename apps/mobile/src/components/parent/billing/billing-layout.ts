import { Spacing } from '@/constants/theme';

/** Vertical gap between major billing blocks (cards, banners, panels). */
export const BILLING_SECTION_GAP = Spacing.three;

/** Vertical gap on the billing scroll container (header, banners, panel). */
export const BILLING_PAGE_GAP = Spacing.four;
